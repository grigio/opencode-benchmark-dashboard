(it should use lifetimes ' like this below)
---

fn longer<'a>(a: &'a str, b: &'a str) -> &'a str {
    if a.len() >= b.len() {
        a
    } else {
        b
    }
}

fn main() {
    println!("{}", longer("gatto", "cammello")); // => cammello
}
